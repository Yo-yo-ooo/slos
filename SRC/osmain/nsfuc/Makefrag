#Nasm函数库

OBJDIRS += nsfuc

NASMFUC_SRCFILES += $(wildcard nsfuc/*.asm)
NASMFUC_OBJFILES := $(patsubst nsfuc/%.asm, $(OBJDIR)/nsfuc/%.o, $(NASMFUC_SRCFILES))

$(OBJDIR)/nsfuc/fuc.o: nsfuc/function.asm 
	@echo + LINKING++++NASM++++
	@echo + nasm[USER] $<
	@mkdir -p $(@D)
	nasm -f elf $< -o $@